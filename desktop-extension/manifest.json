{
  "manifest_version": "0.3",
  "name": "toolbelt",
  "display_name": "Toolbelt — Use Your Org's Agents",
  "version": "0.7.0",
  "description": "Connect Claude to your governed Toolbelt org, pick the optimal model per task, and route work to its agents. Bundles the router behavior so there's nothing to paste.",
  "author": {
    "name": "Apexti",
    "url": "https://apexti.com/toolbelt"
  },
  "homepage": "https://apexti.com/toolbelt",
  "server": {
    "type": "node",
    "entry_point": "bridge.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/bridge.js"],
      "env": {
        "TOOLBELT_MCP_URL": "https://toolbelt.apexti.com/api/workspaces/${user_config.toolbelt_workspace_id}/mcp",
        "TOOLBELT_API_KEY": "${user_config.toolbelt_api_key}"
      }
    }
  },
  "user_config": {
    "toolbelt_workspace_id": {
      "type": "string",
      "title": "Hub workspace (assistant) ID",
      "description": "Your Toolbelt hub workspace (assistant) ID — the agent whose endpoint lists your org roster and routes work.",
      "required": true
    },
    "toolbelt_api_key": {
      "type": "string",
      "title": "Toolbelt API key",
      "description": "Your Toolbelt API key. Stored in your OS keychain (sensitive); sent as an Authorization: Bearer header, never in a URL.",
      "sensitive": true,
      "required": true
    }
  }
}
